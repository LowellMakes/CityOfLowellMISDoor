var React = require('react');
var ReactDOM = require('react-dom');
var App = require('./components/App');
var appNode = document.getElementById('root');

ReactDOM.render(React.createElement(App), appNode);